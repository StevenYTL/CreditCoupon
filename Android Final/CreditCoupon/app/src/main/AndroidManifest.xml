<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.com.creditcoupon">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"

            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrackActivity"></activity>
        <activity android:name=".NoticeActivity"></activity>
        <activity android:name=".MyActivity"></activity>
        <activity android:name=".MainActivity_detail"></activity>
        <activity android:name=".TrackActivity_bank_mount"></activity>
        <activity android:name=".TrackActivity_bank_mount_category"></activity>
        <activity android:name=".EditProfile"></activity>
    </application>

</manifest>